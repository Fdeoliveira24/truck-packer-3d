# Review Queue

Files in this folder are not authoritative yet.

Use this folder for older state reports, diagnostics, bug-fix notes, and documentation that may still contain useful context but needs review before it guides implementation.

When a file is reviewed, either promote it to `docs/product/`, `docs/dev/`, or `docs/audits/`, or move it to `docs/archive/`.
