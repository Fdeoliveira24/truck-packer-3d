# Archive

Files in this folder are historical.

They are kept for context, debugging history, and traceability, but they should not guide new implementation unless a current `docs/product/`, `docs/dev/`, or `docs/audits/` file explicitly references them.
